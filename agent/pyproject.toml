[project]
name = "vllm-omni-playground-agent"
version = "0"
requires-python = ">=3.10"
dependencies = [
    "livekit-agents[openai,silero]>=1.6",
    "python-dotenv>=1.0",
]

[tool.uv]
# a standalone script collection, not an installable distribution
package = false
